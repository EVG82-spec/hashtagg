# codemagic.yaml
workflows:
  ios-workflow:
    name: iOS Build
    max_build_duration: 60
    environment:
      flutter: stable
    scripts:
      - flutter build ios --release
    artifacts:
      - build/ios/ipa/*.ipa